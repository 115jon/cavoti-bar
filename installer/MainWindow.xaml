<Window x:Class="CavotiInstaller.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cavoti Bar Setup" Height="400" Width="320"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="True" ResizeMode="NoResize"
        Icon="Assets/icon.ico" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Window.Resources>
        <Style TargetType="ProgressBar">
            <Setter Property="Foreground" Value="#8F4A22" />
            <Setter Property="Background" Value="#EAD2BD" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Padding" Value="14,7" />
            <Setter Property="Margin" Value="5,0" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </Window.Resources>
    <Border Background="#F7F3EE" BorderBrush="#EAD2BD" BorderThickness="1" CornerRadius="16" Padding="28">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
                <TextBlock Text="CAVOTI BAR" Foreground="#8F4A22" FontFamily="Segoe UI" FontSize="11" FontWeight="Bold" />
                <TextBlock Text="Desktop usage monitor" Foreground="#6B5F54" FontFamily="Segoe UI" FontSize="12" Margin="0,4,0,0" />
            </StackPanel>
            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border Width="128" Height="128" Background="#F3E4D6" CornerRadius="24" Padding="18" HorizontalAlignment="Center">
                    <Image Source="Assets/icon.png" Stretch="Uniform" SnapsToDevicePixels="True" UseLayoutRounding="True" />
                </Border>
                <TextBlock x:Name="Status" Text="Starting..." Foreground="#1C1712" FontFamily="Segoe UI" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,22,0,0" />
                <TextBlock x:Name="Details" Foreground="#6B5F54" FontFamily="Segoe UI" FontSize="12" TextWrapping="Wrap" TextAlignment="Center" Width="230" Margin="0,8,0,0" Visibility="Collapsed" />
            </StackPanel>
            <StackPanel Grid.Row="2">
                <ProgressBar x:Name="Progress" Width="220" Height="7" IsIndeterminate="True" HorizontalAlignment="Center" Margin="0,0,0,16" />
                <StackPanel x:Name="Actions" Orientation="Horizontal" HorizontalAlignment="Center" Visibility="Collapsed">
                    <Button Content="Cancel" Width="92" Height="34" Background="#EEE8E0" Foreground="#6B5F54" Click="Cancel_Click" />
                    <Button x:Name="Confirm" Content="Uninstall" Width="92" Height="34" Background="#B23B3B" Foreground="White" Click="Confirm_Click" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
