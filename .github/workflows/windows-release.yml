name: Cavoti Windows Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  desktop-release:
    runs-on: windows-latest
    env:
      CARGO_INCREMENTAL: 0

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate release tag and version
        shell: pwsh
        env:
          RELEASE_REF: ${{ github.ref }}
        run: |
          if ($env:RELEASE_REF -notmatch '^refs/tags/v(?<version>[0-9]+\.[0-9]+\.[0-9]+)$') {
            throw "This workflow only signs semantic-version release tags."
          }
          $config = Get-Content -Raw -LiteralPath "src-tauri/tauri.conf.json" | ConvertFrom-Json
          if ($Matches.version -ne $config.version) {
            throw "Release tag version $($Matches.version) does not match tauri.conf.json version $($config.version)."
          }

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup .NET Framework build tools
        uses: microsoft/setup-msbuild@v2

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build custom Cavoti installer
        shell: pwsh
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        run: .\scripts\build-tauri-installer.ps1

      - name: Publish setup and updater metadata
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          $installer = "installer/bin/Release/net48/Cavoti Bar Setup.exe"
          $signature = "$installer.sig"
          if (-not (Test-Path -LiteralPath $installer)) { throw "Custom installer not found: $installer" }
          if (-not (Test-Path -LiteralPath $signature)) { throw "Custom installer signature not found: $signature" }

          $config = Get-Content -Raw -LiteralPath "src-tauri/tauri.conf.json" | ConvertFrom-Json
          $version = $config.version
          $assetName = "Cavoti Bar Setup.exe"
          $encodedAssetName = [Uri]::EscapeDataString($assetName)

          $metadata = [ordered]@{
            version = $version
            notes = "Cavoti Bar $version"
            pub_date = (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
            platforms = [ordered]@{
              "windows-x86_64" = [ordered]@{
                signature = (Get-Content -Raw -LiteralPath "installer/bin/Release/net48/$assetName.sig").Trim()
                url = "https://github.com/${{ github.repository }}/releases/download/$env:RELEASE_TAG/$encodedAssetName"
              }
            }
          }
          $latestPath = "installer/bin/Release/net48/latest.json"
          $metadata | ConvertTo-Json -Depth 6 | Set-Content -LiteralPath $latestPath -NoNewline

          gh release create $env:RELEASE_TAG `
            "installer/bin/Release/net48/$assetName" `
            "installer/bin/Release/net48/$assetName.sig" `
            $latestPath `
            --title "Cavoti Bar $version" `
            --generate-notes `
            --latest
